# C in browser

This web app shows a textbox in which the user can write C code. When clicking on the button "Run", the backend takes the code and compiles it and show then the result of the console back in another textbox in the html.


